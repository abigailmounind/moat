# 手工探索入口验证

日期：2026-09-11。范围：无 AI 主动手工入口及本地保存；全部使用合成输入。

## 问题与修复

结果输入页“只整理我写下的内容”直接派发 ANALYSIS_SUCCESS，但状态机仅在 analyzing 接受该事件；页面仍在 outcome，因而无法进入确认。此前草稿内容单元测试不能覆盖这个按钮问题。

入口现先进入 ANALYSIS_START，再以手工草稿进入确认，复用已有确认闸门和显式保存流程。没有改变规则映射、存储格式、视觉或外部接入。原有工作区修改保留，未提交或发布。

## 本轮证据

- 新增 `scripts/manual-exploration-dom-check.mjs`：修复前复现停留原页，修复后通过。覆盖主动入口、逐条确认、自动资本/河流关联和方向为零、确认前不写入、存储配额失败保留页面、重新保存成功、重复保存不增加档案内容。保存时间允许更新。
- `workspace-dom-check.mjs` 与 `stage11-dom-check.mjs` 通过：路径归属、计划/里程碑、成长记录、成果确认、地图详情、撤回及失败边界。两套检查使用各自合成夹具，不是单个浏览器端到端会话。
- 修改前基线 `npm test` 的 24 个测试文件通过；修改后 `npm run check` 的 30 个浏览器模块语法及入口资源检查通过。
- jsdom 路径本轮核实为 `/tmp/moat-dom-validation/node_modules/jsdom/lib/api.js`；复跑命令见根 README。

## 真实浏览器闭环

连接中断后改用断点续传，将 Playwright Chromium Headless Shell 与缺少的运行库隔离在 `/tmp`；未修改系统安装或生产依赖。新增可复跑的 `scripts/browser-flow-check.mjs`，通过外置 Playwright、浏览器路径和本地服务地址运行。

桌面 1440 × 900 与窄屏 390 × 900 均连续通过：键盘进入手工整理、逐条确认、显式本地保存、自主建立能力路径、路径下建立计划和里程碑、完成行动、记录成长、确认证明、刷新恢复，以及从地图查看证明。两种视口都没有页面异常、意外 `/api/` 请求或横向溢出，并启用了减少动态效果；390px 使用浏览器触控模拟。

自动截图中的中文因临时浏览器没有中文字形而显示方框，因此本轮只确认交互、DOM 文本、视口边界和整体布局，没有完成字体视觉验收。浏览器触控模拟不等于实体设备；用户视觉验收也未发生。规则的可跳过补充核对、服务端分析和账号主线仍按 milestones 推进。
