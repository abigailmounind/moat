# 地图证明上下文验证

日期：2026-09-11。

## 改动

沿用 Pages + localStorage 稳定性路线，修复已登记的跨河证明问题。河流摘要、文字说明及演示方向的证明入口显式传递河流；证明详情的未来方向使用当前入口上下文。全部本地证明列表仍使用证明默认关联，无关联记录不强行分河。

仅调整 app.js 的上下文传递，未修改样式、地图资产、存储格式或领域归属。保留此前成长页与 Pages 脚本等工作区改动。

## 验证

- `node scripts/scaffold-dom-check.mjs /tmp/moat-dom-validation/node_modules/jsdom/lib/api.js` 通过；临时 jsdom 未加入项目依赖。
- 新增合成的多河关联证明与无关联证明，覆盖摘要、文字说明、详情未来方向、Escape 恢复摘要和焦点、全部证明列表默认关联，以及浏览前后存储内容不变。
- 同一 DOM 检查中的探索确认、保存、个人地图、方向转路径及编辑保留回归通过。
- 将旧 app.js 临时用于同一回归时失败，确认可拦截缺失的河流上下文；随后恢复修复版本。
- `npm test`：22 个测试文件通过；`npm run check`：30 个模块语法及入口、资产、动效保护检查通过。

本轮没有布局或视觉变更。DOM 结果不代表真实浏览器、触控或用户验收，完整浏览器闭环仍待走查。本轮没有提交 Git 或部署 Pages。
