# 服务端数据生命周期

日期：2026-09-11。

新增 GET /api/v1/data/export 和 DELETE /api/v1/data。导出复用仓储一致性读取；删除要求同源、当前主体明确确认和当前工作区版本。内存同步删除，PostgreSQL 在工作区锁内事务删除主体及关联数据；所有会话和含历史内容的幂等回执一并移除，成功才清 Cookie。未接 UI，不清浏览器数据或备份。

验证：npm test 的 21 个测试文件通过；npm run check、npm run check:api、git diff --check 通过。真实 HTTP 使用独立临时内存服务及合成会话，验证导出、删除、旧会话失效、其他主体保留。专项还覆盖未确认、主体不符、陈旧版本、读取副本隔离及存储失败不显示成功。

check:postgres 已接入复用的生命周期检查，并核对八张关联表没有被删主体记录。当前缺 MOAT_TEST_DATABASE_URL，未执行真实数据库检查，不能声称级联删除、回滚和并发在数据库已验收。

删除响应丢失后旧会话不可用，不能把 401 当作删除成功证明；不为重放而保存已删除个人内容。下一步包括删除/导出 UI、断线结果核对、本地副本处理、数据库并发与回滚专项、备份恢复。
