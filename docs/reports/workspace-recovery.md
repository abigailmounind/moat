# 工作区连接恢复验证

日期：2026-09-11。

## 修复

连接层此前把提交后的工作区读取错误也视为写入被拒绝，可能清掉已经成功写入请求的待重试记录。现在仅写请求明确拒绝才转为失败草稿；会话预检、成功后的读取以及缓存保存失败，保留原请求和幂等键。

## 验证

新增六项连接层—产品服务—内存仓储集成回归：响应丢失并重建连接后重放、提交前缓存失败、会话失效/主体改变、共享锁下双标签陈旧提交、提交后读取失败、提交后缓存失败、导入响应丢失后确认启用（部分同例组合）。验证保留本地副本、旧快照、原请求键，重放后读取最新服务端版本。

npm test 的 20 个测试文件通过，npm run check、git diff --check 通过。测试使用请求/响应及 Web Locks 替身；仓储为内存，durable 标记仅用于覆盖连接分支，不是持久化证据。

## 限制

本轮未运行真实浏览器、真实 Web Locks 或 PostgreSQL。没有 Web Locks 时的跨标签原子保护仍不足，需下一步收口。数据库检查仍需 MOAT_TEST_DATABASE_URL。页面布局未修改，现有用户数据没有迁移或删除。
