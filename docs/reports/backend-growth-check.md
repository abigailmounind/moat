# 后端成长接口验证

日期：2026-09-11。

核对发现成长、证明确认/撤回及空工作区导入接口已有实现，本轮补齐 HTTP 与仓储专项回归，并修复 selection 为数组时错误接受的问题。

覆盖：显式确认、非法关联、无效输入不占用幂等键、普通编辑保留旧快照、撤回和删除、历史重放不覆盖新数据、陈旧版本、主体隔离、空目标导入及持久模式聚合写禁用。共享仓储案例同时接入内存测试和真实 PostgreSQL 检查脚本。

验证：npm test 的 19 个测试文件全部通过；npm run check、npm run check:api、git diff --check 通过。HTTP 检查覆盖 29 个浏览器模块及原有会话/幂等回归，成长 HTTP 专项采用 Node 请求/响应替身。

当前未设置 MOAT_TEST_DATABASE_URL；真实 PostgreSQL 新专项未执行，不沿用历史数据库验证作为本次验收。未改视觉，无浏览器验收。下一步及剩余范围见 milestones 的后端剩余清单。
