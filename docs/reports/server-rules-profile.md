# 服务端规则分析与确认档案验证

日期：2026-09-11。范围：V1 确定性规则候选、确认变更集、内存仓储与 PostgreSQL 迁移。

本轮新增 `POST /api/v1/analyses/rules`、`GET /api/v1/profile` 和 `POST /api/v1/profile/change-sets`。规则生成与浏览器共用 `shared/rules-analysis.js`，候选仍经过 `shared/understanding.js`；档案变更集经 `shared/profile-changes.js` 校验后按独立 revision 写入。档案回执与工作区回执共用主体幂等容量，重复请求重放原结果，陈旧版本返回当前档案而不覆盖。

已通过：

- `npm test`：25 个测试文件全部通过；新增服务端规则输入、显式河流/方法依据、主体隔离、档案版本、变更集校验、幂等重放和存储失败测试。
- `npm run check`：32 个浏览器/共享模块完成语法与入口、资产、动效约束检查。
- `npm run check:api`：真实内存 HTTP 服务通过模块白名单、规则分析、档案提交、会话隔离、并发工作区重试、导出和删除检查。

PostgreSQL 已加入 `002_profile_writes.sql`，包含档案 revision、档案回执表和删除级联；专项脚本已补充档案事务回滚、重放和陈旧版本用例。

页面默认仍走浏览器本地规则和档案保存。服务端接口已形成可调用边界，尚未接入页面自动同步、探索档案迁移或真实模型。

2026-09-12 复核：再次运行新增专项、全量测试、静态检查、Stage 10 契约样例与真实内存 HTTP 检查，结果分别为 1 个专项文件、25 个测试文件、32 个浏览器/共享模块、1 个契约样例及 31 个 HTTP 加载模块通过。随后使用一次性 PostgreSQL 16.4 隔离测试库运行 `npm run check:postgres`：两版迁移并发应用与校验、主体删除级联、档案 revision、确认幂等与回执失败回滚、成长/证明/导入回滚和重试、写删竞争、跨连接冲突、固定快照、关系变化、回执容量和到期、真实 HTTP 与 Node 重启恢复、会话撤销均通过。测试脚本清理随机 schema，测试容器随后停止并自动删除。
