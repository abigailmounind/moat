# 本地预览与光线修订

日期：2026-09-10。

## 页面空白

对正在运行的 4173 服务实测：首页 200，`/shared/workspace.js` 404。磁盘上的后端已包含共享模块白名单，但旧进程没有加载新代码。停止该旧预览进程并用 `npm start` 重启后，同一模块恢复 200。未迁移或清理浏览器本地数据；后端开发态内存随进程重启清空。

README 增加此类空白的排查与重启说明。无需分别启动前端与后端。

## 发光线

查看实际运行时水彩河面，重新描绘三条主河的选中曲线，使弯折贴合河面，减少穿岸后被 alpha 遮罩截断的片段。细线由 3.5 收至 2 个地图单位，光晕由 34 收至 18，降低不透明度；选中时约 0.75 秒从交汇处向外显现，退出淡出。系统或页面减少动态效果继续立即切换；未增加常驻流纹或新水道。同步导出 `rivers-v2.svg`，运行时仍读取 `map.js`。

用户消息中的参考附件仅显示占位标记，没有可读取图像。本轮依据现有素材修正，尚未完成用户参考图的精确对照。

## 验证

- `npm run check`、`node --check frontend/src/map.js`、`git diff --check` 通过。
- `npm run check:api` 通过：23 个浏览器模块、会话隔离、并发重试与冲突。
- `node scripts/scaffold-dom-check.mjs /tmp/moat-jsdom/node_modules/jsdom/lib/api.js` 通过：个人地图及探索、方向衔接回归。
- Chromium 下载反复连接中断；没有可用的真实浏览器，桌面、窄屏、键盘、触控和减少动态效果的视觉走查未完成。DOM 检查不代表浏览器视觉或用户验收。

本轮保留工作区原有迁移与文档修改，未提交代码。
