# 同步入口修复

日期：2026-09-11。

工作区测试对所有存储键返回同一数据，连接层误判为损坏缓存；修为仅返回工作区键。运行时选中态未改。
补齐已被引用的 sync.js，复用现有连接层实现预览、明确确认连接/复制、取消、刷新、重试和切回本地；保留浏览器数据，探索档案不迁移。补齐 growth-proof.js 静态白名单，并将 HTTP 能力断言对齐当前服务已有的成长接口。

验证：原有 17 个测试文件全部通过；新增 sync-render 测试单独通过，覆盖预览、取消、失败和重复重试保护。真实 HTTP 检查通过，包含 29 个浏览器模块、会话隔离、并发重试及冲突。静态检查和差异空白检查通过。

环境未找到可用浏览器或现成 Playwright/jsdom，未验证布局、窄屏、触控、键盘焦点；未运行真实 PostgreSQL 迁移闭环。页面测试使用替身，不是浏览器验收。既有连接层和成长接口没有在本轮做全面验收，相关架构/API 文档仍需集中核对。下一步优先验证持久库连接、迁移重试及真实浏览器表现。
