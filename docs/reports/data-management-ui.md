# 数据管理页面验证

日期：2026-09-11。

接入数据与同步页：导出 JSON 并显示范围，二次确认才删除服务端主体。成功移除服务器连接缓存，保留原始本地工作区；请求前写入待核对标记，响应丢失时阻止同步与新会话操作。用户可以独立确认清理缓存，界面明确这不确认服务端删除。服务端成功但缓存清理失败分别报告。

验证：全量 21 个测试文件及静态检查通过；之后补充页面删除确认和缓存清理失败测试，各专项通过。覆盖导出后才出现删除入口、取消不提交、断线不显示成功、本地副本保留、陈旧版本拒绝删除、持久标记恢复与独立清理。git diff --check 通过。

测试通过连接层—内存服务及页面/下载替身执行。再次检查未发现 Chromium/Chrome/Firefox 或现成 Playwright/jsdom；未做真实布局、窄屏、键盘、触控及下载落盘验收。PostgreSQL 本轮未执行。未操作用户实际数据。
