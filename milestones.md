# 阶段进度与下一步 · 人生护城河

- 用途：当前状态、验收范围、剩余任务与推进顺序。
- 最后更新：2026-09-13。
- 产品定义见 [PRD](PRD.md)，执行规则见 [AGENTS](AGENTS.md)，专题入口见 [文档导航](docs/README.md)。
- 工程目录与开发规则统一见 [工程规范](docs/ENGINEERING_STRUCTURE.md)，现有 HTTP 与仓储约定见 [API 契约](docs/PRODUCT_API.md)。文档核对不改变历史用户验收范围。

## 1. 当前状态

| 阶段 | 状态 | 已有交付与完成边界 |
|---|---|---|
| 0–8 · 前期产品与设计 | 历史基线已形成 | 产品框架、流程、低保真、主视觉与四态参考已形成；不补记未核实的逐阶段验收，见历史索引 |
| 9 · 我的河核心地图 | 已完成，按用户接受的妥协版本 | 三河总览、摘要、证明、未来方向详情；取消代码生成的常驻水流与未来分叉水道。后续个人数据界面的新增呈现仍需验收 |
| 10 · 探索到地图 | 已完成，限当时本地原型 | 用户确认“我看到了有小圆点也有框。然后10到这里也算完成了”；不包含真实模型、账号或云端。之后新增动态确认卡与方向桥接待复核 |
| 11 · 长期工作区与成果回流 | 已完成，限当前本地产品闭环 | 2026-09-10 用户走完并确认当前闭环通过；路径、计划、里程碑、成长记录及确认快照可运行。不包含账号、云端或真实模型 |
| 12 · 完整体验与系统化 | 进行中，生产受控 AI 已上线 | 根封面、个人地图空态、跨页衔接、匿名 D1 主体、访客统计已部署到正式与隔离环境；生产与隔离环境均有 Free-only AI 受控通道。登录、账号恢复、备份与全面响应式继续建设 |
| 13 · 评测与稳定性 | 进行中，合成回归基础 | 已有领域、存储、DOM 及模拟服务失败案例；真实模型评测、用户研究和发布检查尚未完成 |

“代码可运行”“自动检查通过”不等于用户验收。后续修改单独记录验证范围，不撤销已完成历史版本，也不自动沿用它的验收结论。

2026-09-10 本轮修复：重启旧预览服务后，共享模块恢复加载；河流选中光线按现有水彩河面调整位置、收细光晕并增加柔和显现。参考附件未收到可读取图像，精确对照与用户视觉验收仍待完成。验证范围见 [本地预览与光线修订](docs/reports/preview-highlight-fix.md)。

## 2. 当前能力与未完成项

2026-09-13 已形成并部署根封面与访客统计：`/` 使用不读取个人档案或工作区的基础三河背景，`/?view=river` 承载空态或已确认的个人地图；品牌返回封面。访客事件独立于主体数据，只保留 30 天时间与粗粒度位置，管理明细受 Worker Secret 保护。自动检查、真实本地 D1、Pages Service Binding、隔离迁移与远程 HTTP 检查通过；本轮环境无法下载 Chromium，因此新版封面的 1440px/390px 真实浏览器视觉验收仍待完成。详见 [封面与访客地图预发布](docs/reports/staging-cover-visitor-map.md)。

2026-09-11 手工探索入口修复：结果输入页主动手工整理原先未进入状态机的整理步骤，点击后停留原页；现已接通确认、结果和显式保存。新增 DOM 回归覆盖确认闸门、零自动关联、保存失败、重试与重复保存；工作区及成果回流 DOM 回归通过。真实浏览器在 1440px 和 390px 连续走完手工探索、路径、计划、里程碑、成长、证明确认、刷新及地图回看，没有页面异常、意外 API 请求或横向溢出。临时浏览器缺少中文字形，字体视觉和实体触控仍未验收，详见 [手工入口验证](docs/reports/manual-exploration-entry.md)。

2026-09-11 成长页发布修复：删除 `growth.js` 重复的本地 `readWorkspace` 导入，保留连接层读取及本地/服务器切换。`npm run check` 新增 frontend/src 与 shared 全部模块的 Node 语法检查，已验证能拦截原始重复导入错误；修复后 30 个模块通过。`npm test` 的 22 个测试文件通过，包含成长页实际模块加载与两种工作区来源回归。已按用户授权重新部署 themoat 生产 Pages，线上 HTTP 检查覆盖 7 个入口、29 个模块与 5 个资源并通过；新增受测试约束的静态打包与发布后检查脚本。UI、样式、文案与根 README 未改；真实浏览器操作验收仍待完成，详见 [发布记录](docs/reports/pages-growth-release.md)。该次发布后的路线曾为先稳定 Pages + localStorage，再适配 Workers/D1；本次用户明确调整为先完成 PostgreSQL 验证与数据生命周期，再推进身份与跨设备，Workers/D1 暂不作为主线。

2026-09-11 本地稳定性增量：修复摘要和地图文字说明打开跨河证明时的河流上下文，并让详情的未来方向沿用入口河流。DOM 回归覆盖 Escape 返回、默认关联、无关联及存储不变；22 个测试文件与 30 个模块语法检查通过。未发布，真实浏览器和用户验收待完成，见 [证明上下文验证](docs/reports/map-proof-context.md)。

| 领域 | 当前可用 | 仍需完成 |
|---|---|---|
| 我的河 | 个人确认记录、实际摘要与证明入口；按河同步路径与有效归属计划；独立演示案例 | 新增长文本与节点密度视觉验收；个人支撑强度、河宽与雾映射仍未校准 |
| 探索 | 五步输入、异步本地整理、逐条确认、分屏结果、本地保存 | 内容质量案例；外部模型输出、语义去重、冲突澄清与事实支持评测 |
| 未来路径 | 按河组织、增改删、比较、方向带入草稿、重入已有路径 | 集中交互验收；未来模型重新分析不能覆盖用户编辑 |
| 计划与里程碑 | 新计划单路径归属、继承河流、资本目标、独立里程碑编辑和行动完成 | 河流轴层级、长表单、键盘及窄屏专项验收 |
| 成长记录 | 按时间、河流、路径回看；确认、更新、撤回成果快照 | 历史上下文与原记录追溯集中走查 |
| 数据与模型服务 | 浏览器保存、匿名会话、内存/PostgreSQL 与 D1 仓储、对象 API、事务幂等、导出删除、生产/隔离 Free-only AI 限额 | 账号、匿名转账号、备份恢复、跨设备与真实内容质量评测 |

## 3. 下一步顺序

### 已收口的阶段 11 本地闭环

集中路线：**探索 → 逐条确认 → 保存到个人地图 → 查看河流摘要与方向 → 核对并保存路径 → 路径下建立计划和里程碑 → 记录成果 → 确认证明回流。**

2026-09-10 用户确认已走完且通过，当前页面与本地持久化表现通过。该验收限当前浏览器内的本地产品闭环；下面的边界继续作为后端迁移回归要求：
- 同一路径、计划与成长记录刷新后保留修改；重复建立方向路径不覆盖旧编辑。
- 改变路径所属河流时影响明确；旧独立或跨河计划不自动迁移。
- 添加、编辑、删除里程碑及勾选失败不会产生错误完成状态。
- 修改成长记录保留旧证明快照，重新确认才更新；撤回或删除的影响可理解。
- 窄屏、键盘、触控、减少动态效果和长文本不妨碍核心操作。

技术与产品规则见 [工作区规范](docs/STAGE11_WORKSPACES.md)。后端迁移不得让已通过的流程、数据和失败边界回退。

### 当前主线：完整产品后端与无 AI 双通道

当前 Node 服务、浏览器保存和开发态产品 API 仍是原型边界；可选 PostgreSQL 已提供持久匿名会话与路径/计划对象切片，页面可显式连接工作区；完整产品需要持久服务端身份、访问隔离、数据库、同步、导出删除与 AI 网关。采用模块化单体逐步推进；AI 是可选增强，关闭或故障时继续通过确定性规则、手工录入和用户确认完成核心闭环。

实施顺序：

1. 后端地基及首轮持久化已落地：内存/PostgreSQL 双适配器、版本化迁移、匿名会话、路径/计划对象接口、事务回执及共享归属操作均有回归；详见 [首轮验证](docs/reports/backend-persistence-v1.md)。
2. 本地数据库采用 PostgreSQL；成长记录、证明确认及空工作区导入已有接口；持久模式默认关闭聚合写。成长、证明、导入、导出删除及失败回滚已通过真实 PostgreSQL 专项；继续浏览器端到端与备份恢复。里程碑当前随完整计划更新，对象级独立版本后续扩展。
3. 前端连接层和同步入口已形成：工作区与探索确认档案分别预览、确认复制或切换，失败安全重试并保留本地内容；探索档案只提交确认变更集，不上传原始回答。工作区入口修复见 [同步入口修复](docs/reports/sync-entry-fix.md)。档案连接已在 PostgreSQL 持久服务中通过 1440px 与 390px 真实浏览器联合走查，并覆盖两标签冲突、实际下载、二次确认删除、会话失效与本地恢复，见 [探索档案同步验证](docs/reports/exploration-profile-sync.md)；实体触控和字体细节仍待验收。
4. 服务端规则整理与阿里云百炼新加坡多模型适配骨架已接入 V1：后台白名单路由、Free-only 关闭式配置、额度熔断、日上限、输出校验及显式规则回退已有模拟回归。已增加同会话单在途、默认每小时 6 次的进程内保护，以及 C02、C04–C10、C15 首批固定内容自动评测。用户控制台显示前三次真实请求供应商侧均成功，Flash/Max 平均耗时约 23.9/58.93 秒；据此将默认超时调整为 75 秒。页面现已接入默认关闭、仅限本轮的外部处理同意；不展示模型选择，云端失败明确回退规则；1440px/390px、键盘和零意外请求的模拟服务真实浏览器验收已通过。公开前还需边缘/IP 限流、剩余内容案例与真实模型质量评测。
5. Cloudflare Workers + D1 后端已覆盖匿名会话、档案/完整工作区、导出删除、规则整理和默认关闭的百炼多模型网关。2026-09-12 新增第二版迁移、事务内共享键/容量占用与过期清理，修复同键并发误报冲突；增加公开边缘限流、实际流式请求大小限制、D1 全局每日/个人小时模型名额与在途锁。真实本地 D1 的 HTTP 生命周期、同键并发、过期复用、跨资源容量、故障回滚、模拟模型回退、前端协议和运行时重启持久性通过；无真实模型调用或云端部署。Node + PostgreSQL 保留。当时远程预发布和 Pages 同源联调尚未完成，后续结果见下文；账号和备份恢复仍待完成。见 [发布保护验证](docs/reports/cloudflare-release-guards.md) 与 [早期 D1 验证](docs/reports/cloudflare-d1-local.md)。

6. 2026-09-13 Pages 同源接入本地准备完成：显式 API 打包加入 advanced-mode 适配器及仅覆盖 API 的路由文件，默认静态产物保持不变；通过服务绑定透传来源、会话和幂等语义，绑定故障拒绝回落首页。两个真实本地 Worker + D1 已走完静态入口/模块/素材、数据生命周期、前端连接及丢响应重试、规则整理、导出删除、来源拒绝和 API 限流。新增只读规则版预发布检查、独立 staging 配置模板；没有远程建库/发布/密钥上传。该次本地准备的 31 个测试文件与静态检查通过；后续远程与浏览器结果见下文。本地证据见 [同源接入验证](docs/reports/pages-api-integration.md)。

近期发布推进顺序：2026-09-13 用户确认 Workers Free 后，已按既有授权创建隔离 `moat-staging` Pages/D1 与 `moat-api-staging` Worker，模型关闭、未上传百炼 Key，`themoat` 生产部署未变。远程静态/API 检查、规则整理、前端丢响应重试、D1 数据生命周期，以及 1440px/390px 真实浏览器档案保存、刷新、多标签冲突、下载、删除与本地恢复均通过；合成主体已清理。修复浏览器并发用例的读写时序，未改产品运行代码。生产和预览配置均为 Fail closed、绑定隔离 Worker；本轮部署并验证的是 staging 项目的 main 分支，Preview 分支尚未实际部署验收。详见 [隔离预发布验证](docs/reports/cloudflare-staging-release.md)。账号、备份恢复、实体触控、模型 Free-only 状态和内容质量仍需各自验收；本轮不代表全面开放或用户体验验收通过。

2026-09-13 后续反馈修复：计划页补齐常驻「新建计划」和空工作区按河新建路径入口，仍要求单路径归属；本地/远程桌面与 390px、键盘创建及刷新保存通过。用户确认全部待用模型 Free Quota Only 已生效后，隔离 Worker 上传模型 Secret 并启用本轮同意式云端辅助；修复 workerd 不支持的出站 redirect 参数，补齐输出契约、限制输出及关闭思考；专属端点持续超时后切换同地域标准端点，直连合成结果有效。远程七步合成输入与 HTTP/1.1 真实浏览器 AI 流程通过（约 10/9.3 秒）；当前验证环境 HTTP/2 长请求仍有超时，跨网络复核待完成。详情见 [AI 与计划入口修复](docs/reports/staging-ai-plan-entry.md)。原生产站点保持不变；账号、跨设备恢复、备份演练与完整内容质量评测继续推进。

路径/计划对象接口已复核新计划归属、移河、删除与历史快照保留；聚合 PUT 仅默认开放给内存开发模式，持久模式通过对象接口或显式空工作区导入写入。候选校验已统一到共享层，分析服务解除前端依赖，CLI 复用同一校验；详见 [候选校验共享化](docs/reports/shared-understanding.md)。服务端规则与模型端点现已形成，隔离环境真实百炼通道与用户本轮同意流程已验证；完整内容质量、跨网络稳定性与生产开放仍待验收。

详细边界与验收见 [后端与无 AI 运行架构](docs/BACKEND_ARCHITECTURE.md)。

### 用户确认的后续顺序（2026-09-11）

PostgreSQL 真实环境与数据生命周期 → 账号、匿名转账号、访问隔离和跨设备恢复 → 可独立上线的规则/手工核心 → 真实模型与固定案例评测 → 部署、监控、备份恢复、隐私检查和小范围试用。真实浏览器、窄屏、键盘与异常状态验收贯穿实施。账号认证方式、外部服务与费用在具体接入前确认；本轮未接入或发布。

### 可继续准备的技术工作

以下按当前工程依赖排序；完成项保留范围，后续项不视为已实施：

| 优先顺序 | 工作 | 交付与通过标准 | 依赖 |
|---|---|---|---|
| 1 | 后端契约与领域服务（聚合地基完成） | 共享校验、能力模式、匿名会话、版本、幂等键、异步仓储、失败与访问隔离已有自动检查；对象级接口随持久化切片推进 | 不依赖外部供应商；沿用现有领域规则 |
| 2 | 服务端持久化纵向切片（首轮完成） | PostgreSQL 路径/计划、事务、重试、归属变更与 Node 重启恢复通过；成长/证明独立接口及完整导出删除继续实现 | 本地数据库已确认；认证、托管与迁移方案后续确定 |
| 3 | 固定内容评测案例 | 覆盖信息不足、只有偏好、多经历、矛盾、修改与删除；每例写明可推断和不可推断内容 | 沿用三河五资本及确认原则；无需模型供应商 |
| 4 | AI / 规则双通道 | 模型不可用时规则或手工模式完成闭环；模型结果仍需确认且不覆盖编辑 | 真实模型需确认供应商、数据用途、费用与外部调用 |
| 5 | 账号与完整体验 | 完整引导、身份归属、本地到账号迁移、跨设备与响应式 | 服务端持久化和访问隔离通过 |
| 6 | 真实用户评测与发布准备 | 自愿走查、问题修复、隐私与删除核对、实际案例总结 | 核心体验稳定；对外发布另行核对授权 |

### 2026-09-11 后端剩余清单

1. 持久库生命周期专项已通过：成长创建/编辑/删除、快照确认/撤回、空工作区导入、导出及主体删除；补齐六种操作在回执写入失败后的回滚与原键重试、删除失败回滚及写入/删除竞争。真实 HTTP 验证成长证明与回执跨 Node 重启恢复、导出、陈旧删除拒绝和删除后会话失效。见 [PostgreSQL 生命周期验证](docs/reports/postgres-lifecycle.md)。
2. 迁移与客户端可靠性：响应丢失、提交前后缓存失败、会话失效/换主体、共享锁下多标签冲突及导入重试已有连接层—内存服务集成回归；修复提交后读取失败误清待重试记录。无 Web Locks 时已明确暂停服务器连接操作并保留数据，相关回归通过；真实浏览器及持久库走查仍待完成。见 [连接锁边界](docs/reports/workspace-locks.md)。见 [连接恢复验证](docs/reports/workspace-recovery.md)。
3. 当前匿名主体的服务端导出/删除 API 已实现：版本和确认校验、会话失效、事务级联。21 个测试文件及真实内存 HTTP 检查通过；数据库专项现已通过，见本轮 PostgreSQL 生命周期报告。用户界面已接入导出、范围核对与二次确认删除；缓存清理失败和断线保留状态已有回归。本地原始副本保留；已补跨标签删除/切换后旧快照来源校验，防止旧服务器内容转存本地。见 [跨保存位置保护](docs/reports/workspace-source-guard.md)。下一步做真实浏览器下载/布局验收，再完成备份保留/清理及恢复演练。见 [数据管理页面验证](docs/reports/data-management-ui.md)。见 [服务端数据生命周期](docs/reports/server-data-lifecycle.md)。
4. 无 AI 规则依据规划已形成：十类规则与十六个正反案例。基础收紧、规则追溯、确认展示、手工整理与结果页主动入口均已落地；新增可跳过补充页，只有用户提供具体方法才提议人力资本，只有用户明确选择关系才提议对应河流，可多选或全部保持未知，当前不自动生成未来方向。24 个测试、DOM 专项及桌面/窄屏真实浏览器闭环通过，浏览器覆盖补充依据填写和全部跳过。见 [规则规范](docs/RULES_SPEC.md)、[补充核对验证](docs/reports/optional-evidence-check.md)、[手工入口验证](docs/reports/manual-exploration-entry.md) 与 [审查证据](docs/reports/rules-audit.md)。服务端规则分析与探索确认档案持久化已形成 V1 接口、内存仓储和 PostgreSQL 迁移；新增 25 个测试文件级回归、真实内存 HTTP 与 PostgreSQL 隔离库专项均已通过。见 [服务端规则与档案验证](docs/reports/server-rules-profile.md)。真实模型接入另行确认。
5. 当前会话退出 API 已实现：内存/PostgreSQL 撤销令牌、同源检查、重复退出、失败保留 Cookie，数据与回执保留；真实数据库及 HTTP 重启验证通过，见 [会话退出基础](docs/reports/session-revocation.md)。账号阶段已确定采用托管身份服务 + 标准 OIDC，供应商无关的 state、nonce、PKCE 与声明校验工具及规范已形成，见 [账号与匿名转账号规范](docs/ACCOUNT_IDENTITY_SPEC.md)。页面退出、会话续期/回收、账号归属与跨设备恢复、限流及运行监控仍待完成；具体供应商和外部接入待确认。

本轮修复 selection 数组误接受问题，新增成长/证明/导入回归。19 个测试文件、静态及 HTTP 检查通过；见 [后端成长接口验证](docs/reports/backend-growth-check.md)。

## 4. 各阶段剩余验收标准

**阶段 11：**长期路径和计划能反复编辑；新计划正确继承路径河流；里程碑完成与资本变化分开；成果有来源并经确认回流；删除、失败和陈旧提交可解释。当前本地模型未接入 AI，不能据此声称已验证真实模型的编辑冲突处理。

**阶段 12：**新用户可进入闭环、回访恢复内容；主要页面在目标设备可读可操作；匿名到账号衔接不重复或丢失记录。设计与工程沿用现有体系，不为系统化重写可用核心。

**阶段 13：**明确案例、样本、口径及实测结果；验证事实支持、约束遵守、模型失败、成本、访问隔离与删除。合成回归不能充当真实用户反馈，不编造 KPI、业务成果或发布状态。

持续边界：不自动执行外部行动，不增加资本评分、职业匹配百分比、复杂预测、收费系统或 Agent；未经确认不改变三河主视觉。

## 5. 验证与历史入口

| 记录 | 用途 |
|---|---|
| [后端持久化首轮](docs/reports/backend-persistence-v1.md) | PostgreSQL、路径/计划对象接口、事务回滚、重试、Node 重启与本地流程回归 |
| [工程文档整理核对](docs/reports/engineering-docs-audit.md) | 工程规范、文档职责、路径与当前/规划边界校正；纯文档检查 |
| [后端地基第二轮验证](docs/reports/backend-foundation-v2.md) | 共享校验、幂等重试、异步仓储、HTTP 与本地流程回归 |
| [后续骨架交接](docs/reports/next-foundation.md) | 当时个人地图、动态确认与方向桥接的自动检查和限制 |
| [阶段 11 验证](docs/reports/stage11-verification.md) | 本地工作区与成果快照首轮检查 |
| [阶段 10 走查](docs/reports/stage10-walkthrough.md) | 当时探索、保存与记录入口修复 |
| [阶段 9 验证](docs/reports/stage9-verification.md) | 地图四态及视觉修订过程 |
| [历史归档](docs/archive/README.md) | 阶段 0–8 素材与旧产品定义 |
| [整理前文档快照](docs/archive/docs-before-cleanup-2026-09-10.md) | 旧阶段表、历次修订、纠错与验收原文 |

2026-09-10 后端第二轮切片已运行专项、全量和 DOM 自动检查，并通过独立临时服务的真实 HTTP 冒烟；这不等于持久化、账号、浏览器视觉或用户验收完成。
